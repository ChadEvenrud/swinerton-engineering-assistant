# Copy to .env and fill in locally. Never commit real values.
# Store production credentials in GitHub Actions secrets.

OPENAI_API_KEY=
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
