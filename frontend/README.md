# Frontend

Web UI where an engineer submits a question and later sees the agent's answer.

Suggested stack from the curriculum: React (or similar). Talk to the backend over REST. Do not call the LLM from the browser.
